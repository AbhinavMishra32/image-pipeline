{
  "name": "@image-pipeline/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/worker.ts",
    "build": "prisma generate --schema ../api/prisma/schema.prisma && tsc -p tsconfig.json",
    "start": "node dist/apps/worker/src/worker.js",
    "test": "vitest run",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.864.0",
    "@image-pipeline/core": "workspace:*",
    "@prisma/client": "^6.19.3",
    "bullmq": "^5.58.5",
    "dotenv": "^16.6.1",
    "ioredis": "^5.7.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.0.15",
    "prisma": "^6.19.3",
    "tsx": "^4.20.3",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}

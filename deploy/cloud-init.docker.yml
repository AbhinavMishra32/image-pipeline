#cloud-config
package_update: true
package_upgrade: true
packages:
  - docker.io
  - docker-compose-plugin
  - rsync
  - curl
  - jq
  - git

runcmd:
  - systemctl enable docker
  - systemctl start docker
  - mkdir -p /opt/image-pipeline
